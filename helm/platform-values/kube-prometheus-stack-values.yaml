grafana:
  adminPassword: "LocalGrafana123!"   #fixed password to avoid random secret regeneration on each render

  additionalDataSources:
    - name: Loki
      type: loki
      access: proxy
      url: http://loki.logging:3100
      isDefault: false
      editable: true